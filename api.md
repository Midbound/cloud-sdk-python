# Health

Types:

```python
from midbound_cloud.types import HealthCheckResponse
```

Methods:

- <code title="get /health">client.health.<a href="./src/midbound_cloud/resources/health.py">check</a>() -> <a href="./src/midbound_cloud/types/health_check_response.py">HealthCheckResponse</a></code>

# Webhooks

Types:

```python
from midbound_cloud.types import (
    IdentityResolvedWebhookEvent,
    IdentityQualifiedWebhookEvent,
    IdentityEnrichedWebhookEvent,
    IdentityValidatedWebhookEvent,
    IdentitySessionFinalizedWebhookEvent,
    UnwrapWebhookEvent,
)
```
